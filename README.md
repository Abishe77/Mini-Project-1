# Mini-Project-
This project implements a content-based recommendation system that suggests the most suitable social services (Food, Shelter, Hospital) based on user preferences.

The system represents both services and user input as feature vectors and computes similarity using Euclidean distance. Services with smaller distance scores are considered better matches.
